# PR artifact proof

A successful `Android Build` run must contain an uploaded artifact named `tornado-android-debug`. Verify artifact metadata after the workflow completes; a green configuration test without an APK is insufficient.
