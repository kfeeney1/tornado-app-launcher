# Platform preservation

Adding Android packaging must not regress Tornado Web or Windows. The existing Quality workflow remains a merge gate alongside Android Build. Work Block 2 changes packaging/build infrastructure only; shared application behaviour is intentionally unchanged.
