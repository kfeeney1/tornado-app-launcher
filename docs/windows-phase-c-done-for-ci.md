# Phase C branch preparation complete

Open the PR now. Required CI is the next gate. Fix failures on this branch, merge only green, verify `main`, then create Phase D from that verified merge commit.
