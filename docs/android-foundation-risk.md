# Android foundation risks

The permanent package ID has not yet been approved for store publication, so `ie.tornado.launcher` remains provisional. The native Gradle project is generated in CI in this foundation slice; once the build is green, later work can decide which generated native files should become source-controlled for long-term Android development. Production signing and device acceptance remain deliberately outside this block.
