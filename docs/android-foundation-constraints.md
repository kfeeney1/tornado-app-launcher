# Constraints

Do not create a second React application, duplicate Firebase/auth/sync logic, add broad Android package visibility, introduce a second Firebase environment, weaken Electron/Web checks, or require production credentials merely to compile the Android debug client.
