# Android build command

The CI proof is Gradle `assembleDebug` after the shared Vite build and Capacitor sync. Developers may use `npm run android:build:debug` for the equivalent local path after the native project has been created.
