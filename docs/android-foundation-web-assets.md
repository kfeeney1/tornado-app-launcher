# Shared web assets

Capacitor packages the output of the existing Vite build from `dist`. Android must therefore use the same application bundle that Web/Windows derive from shared source. There is no separate Android UI build tree in this block.
