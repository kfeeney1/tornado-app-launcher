# Regression protection

Work Block 2 does not replace the repository's Web/Windows Quality workflow. Both existing checks and Android packaging checks run on the pull request. Any shared regression introduced by Android tooling must be fixed before merge.
