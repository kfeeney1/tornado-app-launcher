# Capacitor role

Capacitor provides the Android application host and native bridge boundary. Work Block 2 uses it only to package the existing shared Tornado web application. Native plugins or platform-specific features should be introduced later only when parity work demonstrates a real requirement.
