# Android native project

This directory is the home of Tornado's Capacitor Android project. During Work Block 2 the native Gradle project is generated reproducibly from `capacitor.config.json` by CI rather than hand-authored here. Generated native sources will be committed only after the generated project has passed the CI build and its configuration has been reviewed.

Do not add signing keys, service-account credentials, or other secrets to this directory.
