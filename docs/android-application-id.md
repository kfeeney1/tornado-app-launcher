# Android application identity

The build-foundation branch currently uses `ie.tornado.launcher` as the technical application ID so CI can generate and compile a native Android shell.

Treat this identifier as provisional until the Android release/publishing work block verifies the intended long-term package identity. Do not publish this build to an app store or create irreversible production-store state from this foundation artifact.
