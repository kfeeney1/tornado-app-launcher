# Android debug artifact

The Work Block 2 APK is a CI/debug acceptance artifact. It is intended to prove that the shared Tornado application can be packaged as an Android application and to support later feature-parity testing.

It is not a production release, is not store-ready, and must not be represented as signed production software.
