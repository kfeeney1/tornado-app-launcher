# PR sequence

This branch targets `main`. CI failures remain on this branch until corrected. Merge occurs only after the required checks are green. Post-merge main checks are then verified before the Android parity branch is created.
