# Exit condition

Exit Work Block 2 only after the PR's native build proves the debug APK can be produced from a clean checkout and the protected-main merge/post-merge cycle is green. Until then the block remains in progress.
