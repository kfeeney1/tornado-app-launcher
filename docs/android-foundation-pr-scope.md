# PR scope review

The pull request should contain Android build/tooling/configuration/tests/docs only. Any unrelated product change discovered during CI should be avoided unless it is the smallest necessary cross-platform fix for a regression exposed by the build.
