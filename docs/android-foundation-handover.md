# Android foundation handover

After this block is green and merged, Work Block 3 may use the debug-capable Android shell to validate and remediate shared-feature parity. It should not replace the shared product with Android-specific implementations. Any native capability needed for parity should enter through the platform adapter boundary.
