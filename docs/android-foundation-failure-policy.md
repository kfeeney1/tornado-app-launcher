# Failure policy

A red Android build is a product-engineering failure to investigate, not an administrative obstacle. Keep the PR open, inspect the failing job, correct the underlying build or dependency problem, and rerun through a new commit. Do not downgrade the build to advisory status merely to merge.
