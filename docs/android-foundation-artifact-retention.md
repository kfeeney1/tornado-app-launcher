# Android artifact retention

The debug APK is uploaded by GitHub Actions under the artifact name `tornado-android-debug`. It exists to support build evidence and later manual/native acceptance. It is not a GitHub Release asset and must not be promoted as production software.
