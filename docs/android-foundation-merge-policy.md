# Merge policy

The tested PR head is the only acceptable merge candidate. Do not bypass branch protection, merge with failed checks, or push these changes directly to `main`. If the head changes, wait for the new checks before merging.
