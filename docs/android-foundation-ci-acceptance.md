# CI acceptance result

The PR is expected to surface any remaining dependency-lock or Android toolchain mismatch immediately. Such a failure is useful engineering signal and must be fixed on the branch; the presence of a PR does not mean the block is complete.
