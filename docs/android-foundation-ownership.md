# Android build ownership

The Android shell is part of the Tornado product repository and follows the same protected-main pull-request process as Web and Windows. Android build changes must retain shared Quality checks rather than creating an isolated delivery path.
