# Pull-request gate

Do not merge based on documentation or configuration checks alone. The real Android Build workflow must finish successfully and produce its APK artifact, alongside the existing repository gates.
