# PR summary source

Adds the Capacitor Android foundation to the shared Tornado codebase, pins the Android Capacitor toolchain, introduces debug APK CI, adds configuration tests and documents security/release boundaries. Merge requires the existing Quality gate and a successful real Gradle debug APK build. Production signing/publishing and feature parity are later work blocks.
