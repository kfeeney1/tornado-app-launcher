# Post-merge verification

After merge, verify the main branch SHA and the workflows triggered by that SHA. Work Block 3 starts only after main remains green. If Android or shared Quality fails after merge, repair through a new pull request rather than pushing directly to main.
