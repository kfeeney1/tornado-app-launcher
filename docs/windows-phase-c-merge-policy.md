# Phase C merge policy

The assistant is authorised to monitor the Phase C PR, diagnose and fix failures on its branch, and merge it once required checks are green. After confirming the merge on `main`, continue directly to the next permitted roadmap phase without waiting for a separate merge instruction, while respecting the deferred Windows acceptance register.
