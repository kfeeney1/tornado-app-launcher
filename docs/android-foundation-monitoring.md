# PR monitoring

Once opened, monitor every Actions run for the PR head. A failed Quality, CodeQL, Android Foundation Check or Android Build run must be inspected and corrected before merge. Do not rely on a single combined status if an individual workflow is still pending or failed.
