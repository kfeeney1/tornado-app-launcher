# Source of truth

`package.json`, `package-lock.json`, `capacitor.config.json`, the shared Vite source and the Android Build workflow together define the reproducible Android foundation. Local Android Studio state is not authoritative.
