# Test intent

The small Node foundation test protects configuration invariants only. It is supplemental evidence and must not substitute for the real Android Gradle build. The workflow therefore runs both configuration checks and native APK compilation.
