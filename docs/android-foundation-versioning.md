# Android foundation versioning

Work Block 2 does not create a production Android release or independently version Android. Tornado's existing shared product version remains authoritative. Android release/version-code mapping will be established in the release-pipeline block so Web, Windows and Android release state can be aligned deliberately.
