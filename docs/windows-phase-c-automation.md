# Phase C continuous execution

For this roadmap, PR creation is followed by active CI inspection. Required failures are fixed on the phase branch. A green PR is merged. The merge commit on `main` is then verified before the next phase branch is created. This process continues without a separate user prompt for each merge unless a genuine blocking decision or unavailable required acceptance step is reached.
