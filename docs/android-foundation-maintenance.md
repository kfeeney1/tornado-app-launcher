# Maintenance expectation

Once established, Android packaging belongs in routine CI. Future shared changes that break Android compilation should be visible before merge. Later governance work may refine required-check enforcement, but this workflow establishes the build signal now.
