# Android build credentials

The debug build must remain credential-free. Do not add a keystore, signing password, service-account JSON, Play publishing credential or production Firebase secret to make this work block pass. Those concerns require dedicated protected configuration in the release work block.
