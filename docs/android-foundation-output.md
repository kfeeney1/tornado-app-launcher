# Build output

Expected debug APK path: `android/app/build/outputs/apk/debug/app-debug.apk`.

The Android Build workflow treats absence of this file as a failure and uploads it only after Gradle completes successfully.
