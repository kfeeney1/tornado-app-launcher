# Pre-merge final check

Before merge confirm: Quality green; Android Foundation Check green; Android Build green; debug APK uploaded; no secrets in the diff; no unrelated Web/Windows regressions; package identity still marked provisional; PR head is the SHA actually tested.
